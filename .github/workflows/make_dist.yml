on:
  push:
    tags:
    - '*'

jobs:
  make_dist:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Clone aether
        run: cd .. && git clone --recursive https://github.com/gfous/aether
      - name: Prepare Vulkan SDK
        uses: humbletim/setup-vulkan-sdk@v1.2.0
        with:
          vulkan-query-version: 1.3.204.0
          vulkan-components: Vulkan-Headers, Vulkan-Loader
          vulkan-use-cache: true
      - name: Compile and Copy Artifacts
        run: mkdir build && cd build &&
             cmake -DCMAKE_BUILD_TYPE=Release .. &&
             cmake --build . &&
             mkdir dist && cd dist &&
             cp ../Debug/arbiter.exe . &&
             cp ../Debug/arbiter.exp . &&
             cp ../Debug/arbiter.lib . &&
             cp ../aether/Debug/aether.lib . &&
             cp ../aether/vendor/cglm/Debug/cglm.exp . &&
             cp ../aether/vendor/cglm/Debug/cglm.lib . &&
             cp ../aether/vendor/cglm/Debug/cglm-0.dll . &&
             cp ../aether/vendor/glfw/src/Debug/glfw3.lib . &&
             cp ../aether/vendor/nativefiledialog-extended/src/Debug/nfd.lib . &&
             cd .. && tar.exe -a -c -f dist.zip dist
      - name: Add to release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: build/dist.zip
